window.onload = init;


// Job array objects
var jobs = [];
var thisJob = [];
// Init function
function init() {
    const endpoint = './assets/json/data.json';
    // Create httprequest object
    var xhr = new XMLHttpRequest()
    // open the conneciton
    xhr.open('GET', endpoint, true);
    // response type of JSON data
    xhr.responseType = 'json';
    // onload with the open connection
    // xhr.onload = function () {
    xhr.onload = function () {
        // Check the xhr response status
        if (xhr.status === 200) {
            const jobs = xhr.response;
            thisJob = jobs.jobs.filter(job=>job.id ===5)
            console.log(thisJob);
        } else {
            console.log("this is connect error")
        }
    };
    xhr.send(null);

}